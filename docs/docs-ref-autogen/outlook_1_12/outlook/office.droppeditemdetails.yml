### YamlMime:TSType
name: Office.DroppedItemDetails
uid: outlook!Office.DroppedItemDetails:interface
package: outlook!
fullName: Office.DroppedItemDetails
summary: Provides the contents and details of the message or file attachment being dragged and dropped into an add-in's task pane.
remarks: |-
  \[ [API set: Mailbox 1.5](/javascript/api/requirement-sets/outlook/outlook-api-requirement-sets) \]

  To learn more about the drag-and-drop feature and how to implement it across various Outlook clients, see [Drag and drop messages and attachments into the task pane of an Outlook add-in](https://learn.microsoft.com/office/dev/add-ins/outlook/drag-drop-items)<!-- -->.

  #### Examples

  ```TypeScript
  // Link to full sample: https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/outlook/80-events/drag-drop-item.yaml

  function dragAndDropEventHandler(event) {
      Office.context.mailbox.addHandlerAsync(Office.EventType.DragAndDropEvent, (event) => {
      console.log(`Event type: ${event.type}`);

      const eventData = event.dragAndDropEventData;
      console.log(`x-coordinate: ${eventData.pageX}, y-coordinate: ${eventData.pageY}`);
      console.log(`x-coordinate relative to task pane: ${eventData.taskPaneX}, y-coordinate relative to task pane: ${eventData.taskPaneY}`);

      if (eventData.type == "drop") {
        console.log("Items dropped into task pane.");
        const files = eventData.dataTransfer.files;
        files.forEach((file) => {
          const content = file.fileContent;
          const name = file.name;
          const fileType = file.type;
          console.log(`File name: ${name}`);
          console.log(`File type: ${fileType}`);
          console.log(`Contents: ${content.text().then((text) => { console.log(text); })}`);
        });
      }
    });
  }
  ```
example: []
isPreview: false
isDeprecated: false
type: interface
properties:
  - name: fileContent
    uid: outlook!Office.DroppedItemDetails#fileContent:member
    package: outlook!
    fullName: fileContent
    summary: Gets the contents of the file being dragged and dropped.
    remarks: |-
      \[ [API set: Mailbox 1.5](/javascript/api/requirement-sets/outlook/outlook-api-requirement-sets) \]

      For guidance on how to retrieve data from a `Blob`<!-- -->, see the [File API documentation](https://developer.mozilla.org/docs/Web/API/Blob)<!-- -->.
    example: []
    isPreview: false
    isDeprecated: false
    syntax:
      content: 'fileContent: Blob;'
      return:
        type: Blob
  - name: name
    uid: outlook!Office.DroppedItemDetails#name:member
    package: outlook!
    fullName: name
    summary: Gets the name of the file being dragged and dropped.
    remarks: '\[ [API set: Mailbox 1.5](/javascript/api/requirement-sets/outlook/outlook-api-requirement-sets) \]'
    example: []
    isPreview: false
    isDeprecated: false
    syntax:
      content: 'name: string;'
      return:
        type: string
  - name: type
    uid: outlook!Office.DroppedItemDetails#type:member
    package: outlook!
    fullName: type
    summary: Gets the type of the file being dragged and dropped.
    remarks: |-
      \[ [API set: Mailbox 1.5](/javascript/api/requirement-sets/outlook/outlook-api-requirement-sets) \]

      \*\*Important\*\*: When a message is dragged into an add-in's task pane, it's dropped as a .eml file.
    example: []
    isPreview: false
    isDeprecated: false
    syntax:
      content: 'type: string;'
      return:
        type: string
