### YamlMime:TSType
name: Excel.BindingDataChangedEventArgs
uid: excel!Excel.BindingDataChangedEventArgs:interface
package: excel!
fullName: Excel.BindingDataChangedEventArgs
summary: Provides information about the binding that raised the data changed event.
remarks: |-
  [API set: ExcelApi 1.2](/javascript/api/requirement-sets/excel/excel-api-requirement-sets)

  #### Used by

  - [Excel.Binding](/javascript/api/excel/excel.binding): [onDataChanged](/javascript/api/excel/excel.binding#excel-excel-binding-ondatachanged-member)

  #### Examples

  ```TypeScript
  // Link to full sample: https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/excel/30-events/data-changed.yaml

  async function registerDataChangedHandler() {
      await Excel.run(async (context) => {
          const sheet = context.workbook.worksheets.getItem("Sample");    
          const salesTable = sheet.tables.getItem("SalesTable");
          const salesByQuarterBinding = context.workbook.bindings.add(salesTable.getRange(), "Table", "SalesByQuarter");
          salesByQuarterBinding.onDataChanged.add(onSalesDataChanged);

          console.log("The data changed handler is registered.");

          await context.sync();
      });
  }

  async function onSalesDataChanged(eventArgs: Excel.BindingDataChangedEventArgs) {
      await Excel.run(async (context) => {
          console.log("Data was changed with binding " + eventArgs.binding.id);
          
          // Get the name of the table that's changed.
          const table: Excel.Table = context.workbook.bindings.getItem(eventArgs.binding.id).getTable();
          table.load("name");

          await context.sync();
          console.log("Name of the changed table: " + table.name);
      });
  } 
  ```

isPreview: false
isDeprecated: false
type: interface
properties:
  - name: binding
    uid: excel!Excel.BindingDataChangedEventArgs#binding:member
    package: excel!
    fullName: binding
    summary: Gets a temporary `Binding` object that contains the ID of the `Binding` object that raised the event. Use that ID with `BindingCollection.getItem(id)` to get the binding.
    remarks: '[API set: ExcelApi 1.2](/javascript/api/requirement-sets/excel/excel-api-requirement-sets)'

    isPreview: false
    isDeprecated: false
    syntax:
      content: 'binding: Excel.Binding;'
      return:
        type: <xref uid="excel!Excel.Binding:class" />
