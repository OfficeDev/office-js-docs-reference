### YamlMime:TSEnum
name: Word.ExportCreateBookmarks
uid: word!Word.ExportCreateBookmarks:enum
package: word!
fullName: Word.ExportCreateBookmarks
summary: Specifies the type of bookmarks to create during export.
remarks: |-
  [API set: WordApiDesktop 1.4](/javascript/api/requirement-sets/word/word-api-requirement-sets)

  #### Used by

  - [Word.DocumentExportAsFixedFormat2Options](/javascript/api/word/word.documentexportasfixedformat2options): [createBookmarks](/javascript/api/word/word.documentexportasfixedformat2options#word-word-documentexportasfixedformat2options-createbookmarks-member)
  - [Word.DocumentExportAsFixedFormat3Options](/javascript/api/word/word.documentexportasfixedformat3options): [createBookmarks](/javascript/api/word/word.documentexportasfixedformat3options#word-word-documentexportasfixedformat3options-createbookmarks-member)
  - [Word.DocumentExportAsFixedFormatOptions](/javascript/api/word/word.documentexportasfixedformatoptions): [createBookmarks](/javascript/api/word/word.documentexportasfixedformatoptions#word-word-documentexportasfixedformatoptions-createbookmarks-member)

  #### Examples

  ```TypeScript
  // Link to full sample: https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/word/99-preview-apis/export.yaml

  await Word.run(async (context) => {
    // Choose whether the exported PDF uses Word headings, named bookmarks, or neither.
    const bookmarkMode = (document.getElementById("bookmark-mode") as HTMLSelectElement).value;
    let createBookmarks: Word.ExportCreateBookmarks;

    switch (bookmarkMode) {
      case "Headings":
        createBookmarks = Word.ExportCreateBookmarks.headings;
        break;
      case "WordBookmarks":
        createBookmarks = Word.ExportCreateBookmarks.wordBookmarks;
        break;
      default:
        createBookmarks = Word.ExportCreateBookmarks.none;
        break;
    }

    // Export the current document as a PDF saved in your default location, usually Documents.
    const filename = `exported-doc-${bookmarkMode}-${getCurrentDate()}.pdf`;
    console.log(`File name: ${filename}`);
    const options: Word.DocumentExportAsFixedFormatOptions = {
      item: Word.ExportItem.documentContent,
      openAfterExport: true,
      optimizeFor: Word.ExportOptimizeFor.onScreen,
      createBookmarks: createBookmarks,
    };

    // If a file with the same name already exists at the default location, it's silently overwritten.
    context.document.exportAsFixedFormat(filename, Word.ExportFormat.pdf, options);

    await context.sync();
  });
  ```

isPreview: false
isDeprecated: false
fields:
  - name: headings
    uid: word!Word.ExportCreateBookmarks.headings:member
    package: word!
    summary: |-
      Creates bookmarks from headings.

      * [API set: WordApiDesktop 1.4](/javascript/api/requirement-sets/word/word-api-requirement-sets)
    value: '"Headings"'
  - name: none
    uid: word!Word.ExportCreateBookmarks.none:member
    package: word!
    summary: |-
      Do not create bookmarks.

      * [API set: WordApiDesktop 1.4](/javascript/api/requirement-sets/word/word-api-requirement-sets)
    value: '"None"'
  - name: wordBookmarks
    uid: word!Word.ExportCreateBookmarks.wordBookmarks:member
    package: word!
    summary: |-
      Creates bookmarks from Word bookmarks.

      * [API set: WordApiDesktop 1.4](/javascript/api/requirement-sets/word/word-api-requirement-sets)
    value: '"WordBookmarks"'
